# M0 · 世界观校准 + 环境搭建

> 边做边记：踩坑 + 命令 + 理解。里程碑：能画出分层架构说清每层职责；`ros2` 命令在环境里跑通。

## 一、世界观校准（读 + 想，不动手）

- [ ] sense→plan→act 闭环：这个循环是机器人的心跳，理解它和"事件驱动异步回调"的区别
- [ ] 系统分层异构：见 `../00-领域地图.md` 第三节，能自己复述每层
- [ ] 仿真=开发环境：机器人的"本地跑一遍"就是仿真

**自测**：合上文档，画一张一台机械臂的分层架构图（实时控制层 / 中间件层 / 感知规划层 / 应用层），标注每层语言和实时性要求。画不出来说明没吃透。

## 二、环境搭建（动手）

选型：Docker 跑 Ubuntu + ROS2（推荐）/ Ubuntu 虚拟机。

> 具体命令、镜像、踩坑记录做的时候往下写 —— 例如：
> - ROS2 版本选哪个（Humble / Jazzy）、为什么
> - Apple Silicon 上 Docker 跑 x86 还是 arm 镜像的坑
> - GUI 工具（rviz/gazebo）在容器里怎么显示（X11 转发 / VNC）

### 环境信息（待填）
- ROS2 版本：
- 运行方式：
- 验证命令：`ros2 --help` / `ros2 run demo_nodes_cpp talker`

## 三、踩坑记录

<!-- 遇到就记，附命令和解法 -->
