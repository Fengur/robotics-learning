# 学习路线 —— 地基阶段

> **活文档**：模块清单 + 里程碑 + 当前进度都在这。进度以**里程碑**衡量，不锁死日期（探索性质，锁时间是压力不是动力）。
> 可视化浏览入口：同目录 `index.html`（双击即开，进度看板与此文档同步维护）。

---

## 目标与边界

**目标**：吃透机器人工程区别于应用工程的核心工作流。不是"学会开机器人"，是——
1. 建立正确心智模型（sense→plan→act、分层异构、仿真=开发环境）
2. 能独立在仿真里跑通感知-规划-控制闭环
3. 看懂系统每一层在传什么、为什么这么设计

**边界**：纯软件、零硬件采购。地基阶段基本用不到 GPU（RL 大规模训练是后续甜区阶段的事，到时按需上云）。

**方向锚点**：M3 走**机械臂抓取**而非移动导航——因为甜区是数据链路 + VLA，而 VLA/模仿学习的主战场是操作(manipulation)，LeRobot 那套也是机械臂。这条线地基直接通向甜区。

---

## 环境底座（第一个动手任务）

ROS2 在 macOS（尤其 Apple Silicon）官方支持弱。主流做法二选一：
- **Docker 跑 Ubuntu 容器**（推荐，轻、可复现、好清理）
- Ubuntu 虚拟机（UTM / Parallels）

MuJoCo、Gazebo 在此环境都能跑。GPU 到甜区阶段再按需上云。
> 详细搭建步骤见 `notes/M0-环境搭建.md`（做的时候写）。

---

## 模块清单

| 模块 | 学什么 | 里程碑（吃透的标志） | 状态 |
|------|--------|---------------------|------|
| **M0 世界观校准 + 环境** | sense→plan→act 闭环、分层异构、仿真=开发环境；搭好 ROS2 环境 | 能画出分层架构说清每层职责/实时性/语言；`ros2` 命令跑通 | ⬜ 未开始 |
| **M1 ROS2 中间件**（重头） | 节点/话题/服务/动作/参数、消息与 QoS、**TF2 坐标变换**、bag 录制回放 | 手写多个节点互相通信 + 录一段 bag 回放 + 可视化 TF 树 | ⬜ 未开始 |
| **M2 仿真闭环** | URDF 建模、传感器仿真、控制接口；Gazebo(ROS原生) 入门 + MuJoCo(接触/为RL铺路) | 仿真里让机械臂动起来：订阅传感器、发控制指令、闭环跑通 | ⬜ 未开始 |
| **M3 操作栈（机械臂抓取）** | 正/逆运动学、MoveIt 运动规划、抓取 pipeline | 仿真里独立完成一次"看到物体→规划→抓取"任务 | ⬜ 未开始 |
| **M4 数学/控制**（横切） | 刚体运动、旋转表示(欧拉角/四元数/旋转矩阵)、坐标变换、PID→状态空间基础 | 不单独立项，跟着 M1-M3 缺什么补什么，笔记里沉淀 | ⬜ 横切进行 |

**贯穿铺垫**：M2/M3 里顺带理解"仿真里怎么训策略"（RL/模仿学习流程），只开头，为后续甜区（数据链路 + VLA / LeRobot）接线，地基阶段不深入。

---

## 里程碑总线（宏观进度）

- [ ] **里程碑 1**：环境就绪 + 世界观校准（M0 完成）
- [ ] **里程碑 2**：ROS2 通信闭环打通，看懂节点在传什么（M1 完成）
- [ ] **里程碑 3**：仿真里让机械臂动起来（M2 完成）
- [ ] **里程碑 4**：仿真里独立完成一次抓取任务（M3 完成）
- [ ] **里程碑 5**：地基复盘——能讲清整条工作流，决定甜区分叉方向

> 完成一个里程碑后，来 `复盘.md` 写一次"现在能做什么 / 还不懂什么 / 下一步"。

---

## 学完地基之后（预告，不在本阶段展开）

三个工程落点，地基扎实后择一纵深（当前倾向按①）：
1. **数据链路工程**（第一推荐，最吃长板）
2. **仿真/评测基建 + Real2Sim**
3. **具身智能体任务层（LLM/VLM 做规划）**

以及物理硬件（LeRobot SO-101 机械臂主从臂 + 二手 GPU 台机）——**留到确认长期投入、需要验证真机差异时再谈，现在不动。**
